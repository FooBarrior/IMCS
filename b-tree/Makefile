sources = main.c linear_sequence_assoc.h tree.c test.c
binary = tree
$(binary): $(sources)
	gcc -o $(binary) -g3 $(sources)
clean:
	rm $(binary)
copy:
	cp $(sources) Makefile /media/DATA/pile/projects/git/IMCS/
